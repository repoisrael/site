{
  "name": "StemRip Local",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.11-bookworm",
  "forwardPorts": [8000],
  "portsAttributes": {
    "8000": {
      "label": "StemRip Local",
      "onAutoForward": "openPreview"
    }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y ffmpeg build-essential && python -m pip install --upgrade pip && pip install -r requirements.txt",
  "postAttachCommand": "echo 'Run: uvicorn app.main:app --host 0.0.0.0 --port 8000'",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python"]
    }
  }
}
